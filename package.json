{
  "private": true,
  "scripts": {
    "test": ":",
    "prebuild": "mkdirp dist",
    "build": "ecmarkup --verbose spec.html dist/index.html",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "ecmarkup": "^3.12.0",
    "mkdirp": "^0.5.1"
  }
}
